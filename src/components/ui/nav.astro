---
import { navLinks, socialLinks } from "@/config/site";
import SocialLinks from "./social-links.astro";
import NavLink from "../ui/nav-link.astro";
import ThemeToggle from "./theme-toggle.astro";
import Logo from "./logo.astro";
---

<nav
  aria-label="Primary"
  class="flex justify-start align-middle container mx-auto h-full [&>*]:p-l flex-row gap-l text-center items-center max-h-3xl"
>
  <Logo />

  <ul
    id="menu-items"
    class="flex-row gap-xl hidden md:flex"
  >
    {
      navLinks.map((link) => (
        <li class="py-1">
          <NavLink class="" href={link.href}>
            {link.title}
          </NavLink>
        </li>
      ))
    }
  </ul>

  <SocialLinks
    class="ml-auto gap-m hidden md:flex"
    links={socialLinks}
  />

  <div class="hidden md:flex">
    <ThemeToggle />
  </div>
</nav>
